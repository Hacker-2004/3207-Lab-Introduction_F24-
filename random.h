
// define a library as global so that the print_random can use the randchar since the function is in different file

#ifndef RANDOM_H
#define RAMDOM_H

// the function name and the type char
char randchar();

#endif